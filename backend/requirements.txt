# PINNED (S726): fastapi 0.139.0 kapselt include_router-Routen als _IncludedRouter
# (nicht mehr flache APIRoute auf App-Ebene). Das bricht die Routen-Introspektion in
# build_scoped_endpoint_inventory (Default-Deny-Türsteher → 0 scope-tragende Routen,
# Runtime-Bug) UND test_scope_wiring. 0.136.1 ist verifiziert gut. Vor Anheben: Code
# an _IncludedRouter anpassen (rekursiv).
fastapi==0.136.1
uvicorn[standard]>=0.32.0
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0
asyncpg>=0.29.0
# psycopg2-binary: synchroner PostgreSQL-Treiber. asyncpg ist async-only;
# get_sync_engine() (db/database.py) baut für die Plus-Modul-Tabellen
# (ensure_plus_db_tables, metadata.create_all) eine synchrone Engine, die auf
# PostgreSQL psycopg2 benötigt. Auf SQLite wird der eingebaute pysqlite genutzt.
psycopg2-binary>=2.9.0
# aiomysql>=0.2.0    # MariaDB/MySQL (bei Bedarf installieren)
pyyaml>=6.0.0
python-multipart>=0.0.9
ansible-runner>=2.4.0
passlib>=1.7.4
bcrypt>=4.0.0
aiosmtplib>=3.0.0
celery[redis]>=5.3.0
redis>=5.0.0
croniter>=2.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
testcontainers[postgres]>=4.8.0
